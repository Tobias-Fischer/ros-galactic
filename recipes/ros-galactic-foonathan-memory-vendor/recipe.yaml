package:
  name: ros-galactic-foonathan-memory-vendor
  version: 1.2.0
source:
  git_url: https://github.com/ros2-gbp/foonathan_memory_vendor-release.git
  git_rev: release/galactic/foonathan_memory_vendor/1.2.0-1
  folder: ros-galactic-foonathan-memory-vendor/src/work

build:
  script:
    sel(win): bld_catkin.bat
    sel(unix): build_catkin.sh
  number: 2
about:
  home: https://www.ros.org/
  license: BSD-3-Clause
  summary: |
    Robot Operating System

extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - "{{ compiler('cxx') }}"
    - "{{ compiler('c') }}"
    - ninja
    - sel(unix): make
    - sel(osx): tapi
    - cmake
    - sel(build_platform != target_platform): python
    - sel(build_platform != target_platform): cross-python_{{ target_platform }}
    - sel(build_platform != target_platform): cython
    - git
  host:
    - foonathan-memory
    - python
    - ros-galactic-ament-cmake-copyright
    - ros-galactic-ament-cmake-lint-cmake
    - ros-galactic-ament-cmake-test
    - ros-galactic-ament-cmake-xmllint
    - ros-galactic-ros-environment
    - ros-galactic-ros-workspace
    - ros2-distro-mutex 0.1 galactic
  run:
    - cmake
    - foonathan-memory
    - python
    - ros-galactic-ros-workspace
    - ros2-distro-mutex 0.1 galactic
    - sel(osx and x86_64): __osx >={{ MACOSX_DEPLOYMENT_TARGET|default('10.14') }}
